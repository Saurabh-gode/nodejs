const { Buffer } = require("node:buffer");

// const memoryContainer = Buffer.alloc(4); // 32 bits
// Buffer.alloc(16, 0);

// console.log(memoryContainer);

// memoryContainer[0] = 0xff;

// console.log(memoryContainer.toString("hex"));

// const newMemoryContainer = Buffer.alloc(3);

// newMemoryContainer.

// newMemoryContainer[2] = 0x21;
// newMemoryContainer[1] = 0x69;
// newMemoryContainer[0] = 0x48;

// const dynamicBuffer = Buffer.from([0x48, 0x69, 0x21]);
// const dynamicBuffer2 = Buffer.from("486921", "hex");
// const dynamicBuffer3 = Buffer.from("Hello World", "utf-8");

// console.log(dynamicBuffer.toString("binary"));
// console.log(dynamicBuffer2.toString("binary"));
// console.log(dynamicBuffer3.toString("binary"));
// console.log(dynamicBuffer3);
// console.log(newMemoryContainer.toString("utf-8"));

// const unsafeBuffer = Buffer.allocUnsafe(10000);

// unsafeBuffer.forEach((element) => {
//   if (element !== 0) {
//     console.log(element);
//   }
// });

// console.log(300 >>> 1);
